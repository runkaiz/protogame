///@function is_whole(number)

// Returns whether or not a number is an interger/whole number.

/// @peram number	The number to check

// Example: if is_whole(HP/2) { *do something* }

// Script by MaxLos :)
//--------------------------------------------------------------------
function is_whole(number)
{
	if (frac(number) = 0) return true; else return false;
}
